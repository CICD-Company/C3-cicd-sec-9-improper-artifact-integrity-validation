name: Improper Artifact and Integrity Validation

on:
  push:

jobs:
  run-tool:
    runs-on: ubuntu-latest
    steps:
      - name: Download external artifact
        run: |
          curl -o tool.sh https://raw.githubusercontent.com/CICD-Company/Artifact/refs/heads/main/tool.sh

      - name: Execute artifact
        run: |
          chmod +x tool.sh
          ./tool.sh
